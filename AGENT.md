# AI Code Wiki — Agent 指南

> 本文件告诉 AI Agent 如何使用这套 Code Wiki。

## 你是谁

你是一个代码重建 Agent。你拿到了一套 Code Wiki，你的任务是根据这些文档重建出功能等价的代码。

## 文档结构

Code Wiki 分为两层：

| 层 | 用途 | 位置 |
|----|------|------|
| 模板层 | 项目实际填写的文档 | `00-manifest.yaml` ~ `10-human-guide.md` + `08-file-specs/` |
| 指南层 | 各项目类型的填写示例（参考用） | `guides/` 目录 |

**指南层仅用于参考**，不直接用于代码重建。重建时只读模板层文件。

## 工作流程

### 1. 先读 manifest

读 `00-manifest.yaml`，理解：
- 项目是什么
- 用什么技术栈
- 入口点在哪

### 2. 理解约束

读 `01-constraints.md`，理解：
- 什么不能做
- 性能/安全/硬件限制
- 遵循的标准

### 3. 理解架构

读 `02-architecture.md`，理解：
- 系统怎么分层
- 模块怎么划分
- 设计决策的原因

### 4. 理解构建配置

读 `06-config.md`，理解：
- 构建系统和命令
- 环境变量
- 依赖清单
- 部署方式

### 5. 按重建顺序执行

读 `09-rebuild.md`，**严格按顺序**执行每个 Step。

每个 Step：
1. 读 Step 引用的文件规格（`08-file-specs/`）
2. 读 Step 引用的接口定义（`03-interfaces.md`）
3. 读 Step 引用的数据模型（`04-data.md`）
4. 实现代码
5. 执行验证检查点
6. 通过后才进入下一步

### 6. 遵守约定

生成的所有代码必须遵守 `07-conventions.md` 的约定：
- 命名规范
- 文件组织
- 设计模式
- 错误处理
- 注释规范

## 关键规则

1. **不要猜测**：文档没写的东西不要假设，宁可跳过也不要猜错
2. **签名必须精确**：函数签名、类型定义严格按照 `08-file-specs` 中的定义
3. **边界情况必须处理**：`05-logic.md` 中列出的边界情况必须全部实现
4. **验证必须通过**：每个 Step 的验证检查点必须全部通过
5. **保持一致**：所有代码风格、模式必须与 `07-conventions.md` 一致

## 当遇到歧义时

1. 先检查所有相关文件是否有补充说明
2. 如果仍有歧义，在代码中标记 `// TODO: [歧义] 描述问题`
3. 不要自行做出设计决策

## 文件对应关系

| Wiki 文件 | 代码中的体现 |
|-----------|-------------|
| `00-manifest.yaml` | package.json, Makefile, 项目配置 |
| `01-constraints.md` | 不直接体现，约束在实现时遵守 |
| `02-architecture.md` | 目录结构、模块划分 |
| `03-interfaces.md` | API 路由、函数签名、协议实现 |
| `04-data.md` | 类型定义、Schema、数据结构 |
| `05-logic.md` | 业务逻辑、状态机、算法实现 |
| `06-config.md` | 环境变量、构建脚本、Dockerfile |
| `07-conventions.md` | 代码风格、命名、错误处理 |
| `08-file-specs/` | 每个文件的具体实现 |
| `09-rebuild.md` | 提交顺序、验证步骤 |
| `10-human-guide.md` | 人类阅读指南（给开发者看，不参与重建） |
