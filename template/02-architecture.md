# 02 — 架构

> 系统怎么切分、为什么这么切分。
> 参考 guides/02-architecture-guide.md 查看各项目类型的架构图示和分层示例。

## 架构概览

<!-- 用 ASCII 或文字描述整体架构。AI 需要一眼看清系统全貌。
     画出主要的组件和数据流方向。
     参考 guides/02-architecture-guide.md 获取对应项目类型的图示模板。 -->

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   组件 A     │────▶│   组件 B    │────▶│   组件 C    │
└─────────────┘     └─────────────┘     └─────────────┘
```

<!-- 根据实际架构替换上面的图 -->

## 分层说明

<!-- 每一层的职责、以及层间调用规则。根据项目类型调整层数和职责 -->

| 层 | 职责 | 可调用 | 不可调用 |
|----|------|--------|----------|
| <层名> | <职责描述> | <可调用的层> | <不可调用的层> |

<!-- 根据实际分层替换 -->

## 模块清单

<!-- 每个模块一句话职责。AI 靠这个决定去哪里找代码 -->

| 模块 | 目录 | 职责 |
|------|------|------|
| <模块名> | `<目录路径>` | <一句话职责> |

## 关键设计决策

<!-- ADR 格式：选了什么、为什么、放弃了什么 -->

### 决策 1：<决策标题>

- **背景**：<需要做出决策的上下文>
- **决策**：<选择了什么方案>
- **理由**：<为什么选这个>
- **放弃方案**：<放弃了什么，为什么>
- **代价**：<这个决策的代价或 trade-off>

## 外部依赖关系图

<!-- 项目依赖的外部服务，以及依赖方向 -->

```
本项目 ──▶ <外部服务 1> (<用途>)
       ──▶ <外部服务 2> (<用途>)
```

## 部署架构

<!-- 运行时的部署拓扑 -->

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   组件 A     │────▶│   组件 B    │────▶│   组件 C    │
└─────────────┘     └─────────────┘     └─────────────┘
```
