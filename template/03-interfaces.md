# 03 — 接口

> 模块之间怎么通信、对外暴露什么能力。
> 参考 guides/03-interfaces-guide.md 查看各项目类型的接口示例。

## 对外接口

<!-- 项目对外提供的接口：HTTP API、CLI 命令、SPI、UART 等。
     根据项目类型选择对应的接口格式，参考 guides/03-interfaces-guide.md。 -->

### <接口类型>

<!-- 每个接口的完整定义。AI 需要精确到可以直接写代码 -->

#### `<接口标识符>`

- **用途**：<接口做什么>
- **鉴权**：<是否需要认证>
- **请求/输入**：
  ```
  <请求格式>
  ```
- **响应/输出**：
  ```
  <响应格式>
  ```
- **错误**：
  - `<错误码>`：<错误描述>
- **实现文件**：`<文件路径>`

---

## 模块间接口

<!-- 模块之间的调用关系和接口定义。用项目语言的接口/协议/函数签名描述 -->

### <模块 A> → <模块 B>

```
<接口定义：函数签名、协议、抽象类等>
```

---

## 消息/事件接口

<!-- 异步通信：消息队列、事件总线、中断 -->

### 事件定义

| 事件名 | 触发时机 | 消费者 | 数据 |
|--------|---------|--------|------|
| `<事件名>` | <触发条件> | <消费者模块> | <数据格式> |

### 中断定义

<!-- 嵌入式项目填写 -->

| 中断源 | 优先级 | 处理函数 | 行为 |
|--------|--------|---------|------|
| <中断源> | <高/中/低> | `<处理函数名>` | <处理行为> |
